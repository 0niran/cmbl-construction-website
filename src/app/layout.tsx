import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "CMBL Construction - Professional Construction Services",
  description: "CMBL Construction offers comprehensive construction services including commercial construction, residential projects, civil engineering, and renovation services across Nigeria.",
  keywords: "construction, commercial construction, residential construction, civil engineering, renovation, Nigeria construction company, CMBL",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}
